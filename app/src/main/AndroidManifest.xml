<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chestnut.webviewtest">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--让所有带有android.intent.action.VIEW action的-->
    <queries>
        <intent>
            <action
                android:name="android.intent.action.VIEW" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WebViewTest"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".brand_new.HomeActivity"
            android:exported="true" >

        </activity>
        <activity
            android:name=".brand_new.WebViewContainerActivity"
            android:exported="false" />
        <activity android:name=".WebViewActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="false">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <activity android:name=".brand_new.HolderActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>